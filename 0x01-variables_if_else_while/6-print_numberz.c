#include <stdio.h>

/**
 * main - This is the main function
 * Return: Always return 0 f successful
 */

int main(void)
{
	int num;

	num = 0;
	while
		(num < 10) {
			putchar(num +'0');
			num++;
		}
	putchar('\n');
	return (0);
}	
