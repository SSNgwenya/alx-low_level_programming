#include <stdio.h>

/**
 * main - This a main function
 * Return: Always return 0 when successful
 */

int main(void)
{
	char num,

	     int eber;

	num = 'a';
	eber = 0;
	while
		(eber < 10) {
			putchar(eber + '0');
			eber++;
		}
	while 
		(num <= 'f') {
			putchar(num);
			num++;
		}
	putchar('\n');
	return (0);
}
