#include <stdio.h>

/**
 * main - This a main function
 * Return: Always return 0 when succesessfu
 */

int main(void)
{
	char v;

	int c;

	v = 'a';
	c = 0;
	while
		(c < 10) {
			putchar(c + '0');
			c++;
		}
	while
		(v <= 'f') {
			putchar(v);
			v++;
		}
	putchar('\n');
	return (0);
}
