#include <stdio.h>

/**
 * main - This a main function
 * Return: Always return 0 when successful
 */

int main(void)
{
	char n,

	int e;

	n = 'a';
	e = '0';
	while
		(e < 10) {
			putchar(e + '0');
			e++;
		}
	while 
		(n <= 'f') {
			putchar(n);
			n++;
		}
	putchar('\n');
	return (0);
}
