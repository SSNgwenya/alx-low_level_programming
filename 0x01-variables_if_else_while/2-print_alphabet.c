#include <stdio.h>

/**
 * main - This is a main function
 * Return: Always return 0 if successful
 */

int main(void)
{
	char c;
	c = 'a';
	while
		(c <= 'z') {putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
