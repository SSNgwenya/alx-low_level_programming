#include <stdio.h>

/**
 * main - This is a main function
 * Return: Always return 0 when successful
 */

int main()
{
	char l;

	l = 'z';
	while
		(l >= 'a') {
			putchar(l);
			l--;
		}
	putchar('\n');
	return (0);
}
